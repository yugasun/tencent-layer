MyLayer:
  # component: '@serverless/tencent-layer'
  component: ../
  inputs:
    region: na-toronto
    name: test
    code: ./node_modules
    bucketConf:
      # key: sls-cloudlayer-test-1584524206.zip
      # bucket: layers
    runtimes:
      - Nodejs8.9
    description: test project layer
    include:
      - includes
    exclude:
      - .bin
    forcePublish: false
